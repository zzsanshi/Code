package 剑指;

public class _67把字符串转换成整数 {
    public int strToInt(String str) {
        str = str.trim();
        int n = str.length();
        if (n == 0) {
            return 0;
        }
        int res = 0;
        return res;


    }
}
